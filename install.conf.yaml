- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    #  ~/.vim: vim
    ~/.zshrc: zshrc
# - create:

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
